const expression = !(((100 % 5) < (100 / 50)) && (300 + 5 === 305));

const myObj = {
    prop1: 'first value',
    prop2: 20
};

const myArray = [40, 50, 2];

const result = myObj.prop1 === (myArray[0] / myArray[2]); // result = true
